import { BaseModule } from "../../base/OldBaseModule";

export default class EconomyModule extends BaseModule {
  constructor() {
    super({
      name: "economy",
      description: "Money!",
    });
  }
}
